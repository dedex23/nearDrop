appId: com.neardrop.app
---
# Test Settings screen visibility and toggle interactions
- launchApp:
    clearState: true
- runFlow: flows/dismiss-permissions.yaml

# Wait for app to fully load
- extendedWaitUntil:
    visible: "Carte"
    timeout: 15000

# Navigate to Settings
- tapOn: "Paramètres"

# Verify settings sections are visible
- assertVisible: "Suivi en arrière-plan"
- assertVisible: "Mode silencieux"
- assertVisible: "Paramètres de notification"

# Toggle Quiet Mode on
- tapOn:
    id: "switch-quiet-mode"

# Scroll to see more settings
- swipe:
    direction: UP
    duration: 500

- assertVisible: "Heures actives"

# Scroll further for data section
- swipe:
    direction: UP
    duration: 500

- assertVisible: "Exporter les données"
- assertVisible: "Importer des données"

# Scroll to About
- swipe:
    direction: UP
    duration: 500

- assertVisible: "NearDrop"

# Scroll back up and toggle Quiet Mode off
- swipe:
    direction: DOWN
    duration: 500
- swipe:
    direction: DOWN
    duration: 500
- swipe:
    direction: DOWN
    duration: 500

- tapOn:
    id: "switch-quiet-mode"
